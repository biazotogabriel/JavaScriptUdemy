export default function soma(x, y) { // exporta essa função como padrão. Pode haver apenas 1 função default
    return Number(x) + Number(y)
}

export const nome = 'Gabriel'
export const sobrenome = 'Biazoto'
export const idade = 30